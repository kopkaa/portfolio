<div class="pill text-sm md:text-lg"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0.2rem 0.75rem;
		gap: 0.2rem;
		color: var(--accent-text-over);
		border: 1px solid var(--accent-regular);
		background-color: var(--accent-regular);
		border-radius: 999rem;
		line-height: 1.25;
		white-space: nowrap;
	}
</style>
